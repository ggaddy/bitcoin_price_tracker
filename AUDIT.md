**Project audit and improvement plan — September 6, 2026**

Baseline: commit `cc4a9fb`. Reviewed the tracked source, configuration, documentation, test runner, and recent history. The working tree was clean at the start. The findings and baseline validation below describe that commit; subsequent implementation progress is recorded in the tracker and change log. P1 is committed as `9bce95b`; P2.1 is implemented and verified, with evidence recorded below.

The project is a compact Rust/Axum application with a self-contained dashboard, four upstream providers, and SQLite persistence. Its module boundaries are reasonable for its size. The most valuable improvements concern refresh reliability and the accuracy of the data shown to users.

**Implementation progress — updated September 6, 2026**

Audit and implementation planning are complete. Implementation: **5 of 30 tasks complete**. Next task: **P2.2 — bound network work and introduce structured provider errors**.

| Phase | Status | Tasks complete | Depends on | Completion evidence |
| --- | --- | --- | --- | --- |
| P1 — Consistent SQLite reads | Complete | 4/4 | None | Race reproduced before the fix; all 5 storage tests pass; full suite 10 passed / 1 ignored; formatting and Clippy pass |
| P2 — Refresh reliability | In progress | 1/7 | P1 | P2.1 complete: local provider fixtures and manual clock; full suite 15 passed / 1 ignored; formatting and Clippy pass |
| P3 — Source freshness and validation | Not started | 0/6 | P2 | — |
| P4 — Dashboard resilience | Not started | 0/6 | P3 for freshness display; lifecycle work can start earlier | — |
| P5 — Delivery and deployment | Not started | 0/7 | Final verification depends on P1–P4; CI can start with P1 | — |

Use `Not started`, `In progress`, `Blocked`, or `Complete` in the phase table. Check a task only when its changes and relevant verification are complete. A blocked task must name its blocker and next action. After each implementation session, update the counts, next task, and change log; include commands/results and commit or PR references when available. Baseline checks do not count as evidence that a later change works.

**Current behavior and strengths**

- Three routes: dashboard, price retrieval, and presence updates. Visible tabs send presence and request prices every five seconds; presence expires after 15 seconds without an update.
- Price requests drive refreshes. The first viewer marks a full refresh pending; subsequent successful updates rotate through providers. Snapshots younger than ten seconds suppress refreshes, including a pending full refresh.
- Only the latest merged snapshot is retained. The recent commits deliberately introduced this behavior; the plan below preserves it.
- SQLite writes use a transaction, parameterized values, and foreign-key cascading. Database operations run through `spawn_blocking`, avoiding synchronous SQLite work on Tokio's async worker threads.
- The HTTP client is reused. Upstream URLs are fixed HTTPS endpoints. Session identifiers have character and length validation. The frontend has no third-party runtime assets.
- Seven ordinary tests cover presence helpers, source ordering, and snapshot replacement/pruning. One browser smoke test is ignored by default. There is no checked-in CI workflow.

**Findings, ordered by priority**

| Priority | Finding and evidence | Impact and proposed correction |
| --- | --- | --- |
| High | Upstream requests have no configured deadline, and the refresh mutex is held across network requests. See `src/main.rs:28`, `src/handlers.rs:55`, and `src/pricing.rs:56`. | A stalled provider can hold the refresh path indefinitely and make active-viewer requests queue instead of returning cached prices. Add connect/total timeouts and allow concurrent readers to receive the stored snapshot while a refresh is already running. Reqwest 0.12.28 documents [no total timeout by default](https://docs.rs/reqwest/0.12.28/reqwest/struct.ClientBuilder.html#method.timeout). |
| High | Provider failure prevents rotation from advancing: `refreshed_source` is persisted only after a successful fetch. Full refresh uses `try_join!`, while `full_refresh_pending` is cleared before success. See `src/pricing.rs:170`, `src/pricing.rs:218`, and `src/handlers.rs:69`. | A failing provider can starve healthy providers. One failure during initial refresh discards the other results; subsequent requests may repeatedly retry CoinGecko. The ten-second gate uses the last successful snapshot, so failures can trigger another upstream attempt on every queued request. Track attempts separately from successful data, advance rotation after attempted work, retain partial successes, and apply bounded backoff, including rate-limit responses. |
| High | Source prices have no individual timestamps; updating one source assigns a new timestamp to the whole merged snapshot. See `src/models.rs:4`, `src/pricing.rs:194`, and `src/pricing.rs:229`. | Old quotes can be displayed as LIVE and included in the average. Even normal rotation updates each source only about once per 40 seconds, plus scheduling/network delay. After a long idle period and failed full refresh, some quotes can be much older. Store last-success time and health per source; define a freshness policy separately from the ten-second request cadence. |
| High | Snapshot metadata and source rows are loaded in separate queries without a read transaction. Writers delete and replace the old snapshot. See `src/storage.rs:104` and `src/storage.rs:162`. | A writer can commit between the queries, leaving the reader with an old average and no matching source rows. Reproduced with the repository schema and two SQLite connections. Load both parts in one read transaction or a single consistent query. |
| Medium | Browser polling allows overlap and has incomplete error handling. `setInterval` invokes an async cycle every five seconds; a presence network failure escapes before `refresh()` handles errors. See `src/ui.html:171`, `src/ui.html:246`, and `src/ui.html:261`. | Slow requests accumulate and responses can overwrite newer or paused UI state. Reproduced three concurrent price requests using the actual script in a Node harness. Use one in-flight price request, bounded fetches, explicit presence error handling, and protection against results arriving after visibility changes. Keep heartbeats independent if price retrieval can exceed the viewer TTL. |
| Medium | Session initialization assumes `crypto.randomUUID()` and session storage are available. See `src/ui.html:158`. | A fresh session on a plain-HTTP LAN address can fail before tracking starts. The API requires a [secure context](https://developer.mozilla.org/en-US/docs/Web/API/Crypto/randomUUID); localhost is a special case. Feature-detect UUID generation, provide an appropriate fallback for this presence identifier, and tolerate unavailable storage. The missing-UUID failure was reproduced in a script harness. |
| Medium | Price semantics and validation are inconsistent: Gemini uses `bid`, Kraken uses the close field, Coinbase uses spot, and CoinGecko uses its aggregate. Numeric parsing is not followed by a finite/positive check. See `src/pricing.rs:56`, `src/pricing.rs:80`, `src/pricing.rs:108`, and `src/pricing.rs:143`. | The displayed average and spread combine different quote types; malformed numeric strings can enter the calculation. Label or standardize quote types, explicitly select BTC/USD, reject invalid prices, and test provider error payloads. Preserve `f64` unless the application later needs accounting-grade arithmetic; changing numeric types alone does not solve these issues. |

Additional improvements:

- **Presence accuracy and resource limits:** the viewer count is captured before waiting for the refresh mutex (`src/handlers.rs:44`). Recheck it before starting upstream work so queued requests do not refresh after viewers leave. Presence is client-reported, and the map has no total entry cap; add bounded storage and request limits for public hosting. Use monotonic time for in-process TTLs/cooldowns. Signed `saturating_sub` does not clamp future snapshot timestamps to zero (`src/presence.rs:41`).
- **Browser presentation and accessibility:** display per-source age and degraded health; retain the last visible price on transient errors; replace variable `innerHTML` interpolation with DOM text nodes (`src/ui.html:224`, `src/ui.html:242`). Current upstream source names are fixed, so this is preventive hardening rather than a demonstrated remote injection vulnerability. Respect reduced-motion preferences and pause the Matrix animation while hidden (`src/ui.html:347`). Replace user-facing database terminology with price-update information.
- **Operations:** the runtime image defaults to root; there is no health route, graceful shutdown, or configurable bind address. Add a non-root user with a writable data volume, useful refresh/error logging, and a local health check that does not call providers. Document that process-local viewers and locking support a single application instance.
- **Build and test maintenance:** use `--locked` in image builds, declare the supported Rust toolchain, and automate formatting, Clippy, tests, and image checks. The Selenium runner uses a floating image tag, requires host Cargo, relies on host networking, and removes any existing container with its configured name. Use an isolated name, guaranteed cleanup, and documented engine/platform assumptions. Its ten-second seeded-price freshness window also makes the test vulnerable to slow browser startup and live upstream access.

**Recommended implementation sequence**

Effort is relative: small means a focused change; medium spans several modules or requires test infrastructure. Implement regression tests alongside the fixes.

| Order | Deliverable | Effort | Acceptance criteria |
| --- | --- | --- | --- |
| 1 | Consistent SQLite reads | Small | Concurrent replacement cannot produce metadata and sources from different snapshots. Existing latest-only retention tests still pass; a failed write retains the prior snapshot. |
| 2 | Reliable refresh coordination and provider isolation | Medium | Slow providers time out; cached reads remain responsive during refresh; healthy providers continue after another fails; all-provider failure preserves existing data; failed attempts observe cooldown/backoff; no new upstream work starts without active viewers. Use injectable endpoints and a controllable clock to test this without public API calls. |
| 3 | Per-source freshness and validated prices | Medium | Persist source timestamps/status with an explicit schema migration. Older snapshots migrate with unknown source age rather than invented fresh timestamps. The API reports partial coverage; the average uses the documented freshness policy; invalid prices cannot replace good data. Test pause/resume and partial-failure recovery. |
| 4 | Resilient, accessible dashboard | Medium | At most one price request is in flight per tab; presence failures produce recoverable UI state; late responses cannot overwrite IDLE; hidden tabs stop polling and animation; reduced motion is respected; session initialization works in supported HTTP/HTTPS environments. Extend browser tests using deterministic provider fixtures. |
| 5 | Repeatable delivery and deployment | Medium | CI runs `cargo fmt --all -- --check`, `cargo clippy --locked --all-targets -- -D warnings`, `cargo test --locked`, and a container build. Verify startup, restart persistence, and shutdown with a non-root data volume. Document configuration, API response semantics, provider assumptions, and the single-instance deployment model. Add dependency advisory checks. |

Steps 1 and 2 offer the highest immediate reliability benefit. Step 3 supplies the data needed for the improved dashboard in step 4. Basic CI can be introduced with the first fix. Historical charts, additional assets, alerts, and streaming delivery can be considered after these reliability changes; history would require an explicit change to the current retention policy.

**Implementation contract and proposed defaults**

These are planning decisions to implement and test, not descriptions of existing behavior. If implementation evidence calls for a different choice, record the reason in the change log.

| Area | Planned behavior |
| --- | --- |
| Application shape | Keep a single Rust service, SQLite, the embedded dashboard, and latest-only quote retention. Keep refreshes request-driven. Use a nonblocking attempt to acquire refresh ownership: the request that acquires it may perform bounded upstream work, while other requests serve the stored snapshot immediately. No background refresh loop is required. |
| Refresh cadence | Keep the ten-second minimum after a successful snapshot. Also enforce at least ten seconds between refresh attempt starts, including failures. Both gates must permit work. Use monotonic time for process-local cooldowns and viewer expiry; use Unix timestamps for persisted observations and display. |
| Network limits | Start with a two-second connect timeout and five-second total timeout per provider. A full refresh runs eligible providers concurrently and retains individual results. Browser price requests get an eight-second deadline; these values are explicit defaults subject to verification. |
| Provider retries | Track next provider, last attempt, consecutive failures, and next eligible time independently of the last successful quote. Advance rotation after an attempt. Use per-provider exponential delays of 10, 20, 40, 80, 160, then 300 seconds; reset on success. Honor a valid `Retry-After` when it requires a longer wait, without sleeping inside the request handler. If all providers are cooling down, serve stored data. |
| First viewer | Mark a full refresh requested on the transition from zero to one active viewer. Preserve that request while the global gates prevent work. A batch attempts every currently eligible provider; failed or cooling-down providers remain eligible for later recovery through their individual retry state. A new viewer activation during a batch must not be erased when that batch finishes. |
| Presence | Keep the five-second heartbeat and 15-second expiry. Recheck active viewers immediately before dispatching upstream work. Work already dispatched may complete and persist its result after the last viewer leaves. Never hold the viewer-map lock across network or database I/O. |
| Freshness | Start with a separate 90-second maximum source age, allowing ordinary four-source rotation plus request/polling delay. Unknown or future observation timestamps are not fresh. Freshness is recomputed on every response; a new snapshot timestamp does not refresh an unchanged source. |
| Aggregate and status | Average/spread use only finite, positive quotes within the freshness window. One qualifying source is enough for an indicative average; return coverage counts. `LIVE` requires all four sources fresh with no outstanding provider failure; partial coverage or a recent provider error is `DEGRADED`; retained quotes with none fresh are `STALE`; no usable quote is `UNAVAILABLE`. Keep the last known quotes visible with their ages. |
| HTTP compatibility | Preserve existing JSON field names during migration. Keep HTTP 200 when a stored quote exists, including a stale quote; use 503 when no usable quote has ever been stored, and 500 for a database read failure. Keep freshness-based `average_price`/`spread` nullable when no quote qualifies. Add an explicit status and coverage fields; document `stale` as true whenever the response is not fully LIVE. |
| Deployment | Support one application process per database. Keep runtime limits process-local and document their reset on restart. Initialize the success-age gate from persisted data so a restart does not immediately refetch a fresh snapshot. |

**P1 — Consistent SQLite reads (4 tasks)**

Files: `src/storage.rs`. Keep this change focused on snapshot consistency and transactional safety.

- [x] **P1.1 — Reproduce the read race.** Add a deterministic test with two connections that attempts replacement between reading snapshot metadata and source rows. Use explicit synchronization or a narrow test seam, not sleeps. A WAL-enabled test database can allow the writer to commit while the reader holds a transaction; enabling WAL in production is not required for this fix.
- [x] **P1.2 — Read one committed snapshot.** Wrap metadata and source queries in one read transaction and fully collect results before finishing it. Preserve empty-database behavior, source ordering, and the public storage API. Keep all synchronous work inside `run_blocking`.
- [x] **P1.3 — Verify rollback and retention.** Force a source insert failure, for example with a test-only SQLite trigger, and assert that the old snapshot and its source rows survive. Retain the existing replacement and startup-pruning tests.
- [x] **P1.4 — Validate and record evidence.** Run formatting, Clippy, and the Rust tests; record the new regression test names and results. Mark P1 complete only when the interleaved read returns a complete old or new snapshot, never a mixture.

P1 implementation evidence (September 6, 2026):

- `src/storage.rs` now holds a deferred read transaction across metadata and source queries, collects all source rows, and finishes the transaction before returning. A private no-op callback in the normal path provides a narrow deterministic interleaving seam for tests. Public APIs, schema, production journal mode, and latest-only retention remain unchanged.
- `load_snapshot_stays_consistent_when_replaced_between_queries` first failed against the original read behavior: the old metadata had zero source rows instead of one. It now passes while a replacement commits during the read and a separate reader observes the new snapshot. WAL is enabled only for this regression's temporary database.
- `failed_source_insert_preserves_previous_snapshot_and_source_rows` forces the second source insert to fail after the first succeeds. It verifies the previous quote, metadata, warnings, and row counts survive, then verifies a subsequent replacement succeeds after removing the trigger.
- `load_empty_database_returns_none_and_allows_subsequent_write` covers the empty-result transaction path. The existing replacement test now inserts sources in reverse order and checks the returned order and values. The startup-pruning test remains passing.
- Checks ran in an ephemeral Rust 1.85.1 container based on the audit builder image, with the current workspace `src` mounted read-only. `cargo test --locked --release storage::tests`: **5 passed**. Final gates: `cargo fmt --all -- --check` passed; `cargo clippy --locked --release --all-targets -- -D warnings` passed; `cargo test --locked --release`: **10 passed, 0 failed, 1 ignored**; `git diff --check` passed on the host. An initial formatting check found import wrapping, which was corrected before these final gates.
- Selenium and final-image deployment checks were not rerun for this storage-only phase. No public provider requests or host Rust installation were needed.

**P2 — Refresh reliability (7 tasks)**

Files: `src/config.rs`, `src/state.rs`, `src/main.rs`, `src/handlers.rs`, `src/presence.rs`, `src/pricing.rs`, `src/util.rs`, and `Cargo.toml`. Add a small `src/refresh.rs` if separating orchestration from provider parsing makes the code easier to follow.

- [x] **P2.1 — Add deterministic test seams.** Inject provider endpoints and a clock through application construction. Use fixed production HTTPS endpoints and local mock HTTP servers in tests; endpoints must not come from public request input. Add only the test dependencies/features needed for controlled delays, time advancement, and HTTP route tests. Record upstream request counts.
- [ ] **P2.2 — Bound network work.** Configure connect and total timeouts in the shared HTTP client. Introduce a provider error type that distinguishes transport/timeout, HTTP status and retry timing, invalid payload, and invalid price; retain useful diagnostics for logs without exposing internal database paths to clients.
- [ ] **P2.3 — Introduce explicit refresh coordination.** Replace success-only scheduling with coordinator state for rotation, attempt timing, and provider retry eligibility. Use `try_lock` or equivalent ownership so another request cannot queue behind upstream work. On contention, return the database snapshot with a refresh-in-progress reason. Capture the pending full-refresh generation so completing an older batch cannot clear a newer activation. Derive response viewer counts from current presence after work completes.
- [ ] **P2.4 — Keep partial successes.** Replace fail-fast all-provider joining with collection of each provider's result. Merge successes with retained quotes and record failures individually. Advance the cursor after an attempted provider even on failure. Keep the previous quote snapshot intact if no provider succeeds; a database failure must not count as a successful refresh or remove attempt cooldowns.
- [ ] **P2.5 — Apply cadence and retry policy.** Implement both global gates and per-provider eligibility from the contract. Skip cooling-down providers when selecting the next source; do not consume an attempt when none are eligible. Parse supported `Retry-After` forms defensively, never retry in a tight loop, and reset failure counts after successful recovery. Keep attempt/retry state in memory; P3 adds persisted health for display.
- [ ] **P2.6 — Correct presence timing.** Use monotonic timestamps for heartbeats and expiry. Recheck presence before upstream dispatch and keep the viewer-map critical section short. Define consistent lock ordering so presence updates cannot deadlock with refresh coordination. Validate at the exact TTL boundary and after hide/show transitions.
- [ ] **P2.7 — Verify the failure matrix.** Add route/coordinator tests for cold start, partial and total provider failure, timeout, rate limiting, retry expiry, rotation after failure, database write failure, concurrent callers, fresh data after restart, and zero viewers. Use barriers to prove a cached response completes while an upstream request remains deliberately stalled. Assert that failures do not increase attempt frequency with viewer count, and that a new activation during a batch remains pending.

P2.1 implementation evidence (September 6, 2026):

- Added `UpstreamEndpoints` in `src/config.rs`, a `Clock` interface with `SystemClock` in `src/util.rs`, and `AppState::with_dependencies` in `src/state.rs`. Normal startup still uses the same fixed HTTPS provider URLs and system time. Endpoint overrides are constructor inputs only, never public request parameters. Provider requests, snapshot timestamps, snapshot ages, and presence expiry now use the injected dependencies.
- Added `src/test_support.rs` with four local Axum provider routes, per-provider request counters, configurable response status/headers/body, semaphore-controlled response release, a manually advanced clock, and isolated temporary SQLite databases. Test applications exercise the real router through Tower; provider adapters use real HTTP against loopback endpoints. Fixture servers and database directories have scoped cleanup. Real test deadlines bound hangs; time-boundary assertions advance the injected clock without sleeps.
- Added four route tests in `src/api_tests.rs`: `no_viewers_never_request_upstreams`, `local_providers_and_manual_time_drive_refresh_and_cache_age`, `manual_time_expires_presence_without_sleeping`, and `held_provider_response_uses_time_at_completion`. They verify all four provider parsers, stored prices, request counts, the nine/ten-second refresh boundary, the 15/16-second viewer-expiry boundary, and completion timestamps after a held response.
- Added `pricing::tests::provider_fixtures_support_http_errors_malformed_json_and_recovery` to exercise an HTTP 429 fixture, malformed JSON, and a subsequent valid response directly through the provider adapter. This test does not require retaining the current fail-fast batch or retry behavior, which later P2 tasks will change.
- Added Tower's `util` feature as a direct development dependency, reusing the already locked version. Declared Tokio `net` and `sync` features explicitly. `Cargo.lock` changes only to record the direct Tower dependency; no package versions changed.
- Final checks ran in an ephemeral Rust 1.85.1 container with current `src` and `Cargo.toml` mounted read-only: `cargo fmt --all -- --check` passed; `cargo clippy --locked --release --all-targets -- -D warnings` passed; `cargo test --locked --release`: **15 passed, 0 failed, 1 ignored**. `git diff --check` passed on the host. No public provider requests were needed; the Selenium test was not run.
- This task preserves existing production scheduling and clock semantics. Production network deadlines belong to P2.2, nonblocking refresh coordination to P2.3, and monotonic presence timing to P2.6. P2 remains in progress.

**P3 — Per-source freshness and validated prices (6 tasks)**

Files: `src/models.rs`, `src/storage.rs`, `src/pricing.rs`, `src/handlers.rs`, `src/config.rs`, and the test fixtures introduced in P2.

- [ ] **P3.1 — Define the source contract.** Add nullable last-success Unix time and quote kind to each stored quote. Define separate last-attempt outcome/error information and computed freshness. Add response status, source age/freshness, configured/fresh source counts, and aggregate coverage. Distinguish persisted observations from computed age so stored summaries cannot be mistaken for current freshness.
- [ ] **P3.2 — Add an atomic schema migration.** Introduce schema versioning, such as SQLite `user_version`, with an explicit path from the existing database. Extend source rows with observation metadata and add one latest health record per provider, including providers with no successful quote. Migrate legacy timestamps as unknown. Keep latest-only retention and make migration repeatable; reject unsupported newer schemas clearly. Preserve the old database on migration failure.
- [ ] **P3.3 — Persist outcomes consistently.** Write successful quote replacements and provider health in one transaction. A failed provider updates its health without overwriting its last good quote or last-success time; an all-failure batch updates health without advancing quote freshness. Read quote and health data together in the consistent transaction established by P1. Load last-known health on restart while keeping monotonic retry scheduling process-local.
- [ ] **P3.4 — Validate provider data.** Validate finite positive values before merging or persisting. Select BTC/USD explicitly for Coinbase and validate the intended pair where upstream payloads identify it. Handle Kraken's error payload explicitly. Keep existing quote types initially but label Gemini bid, Kraken last trade, Coinbase spot, and CoinGecko aggregate; describe the combined value as an indicative average. Add fixtures for missing fields, incorrect pair, provider error, zero, negative, NaN, and infinity values.
- [ ] **P3.5 — Compute response freshness and aggregates.** Evaluate the 90-second policy at response time, exclude unknown/future/expired quotes from the average and spread, and expose retained stale quotes separately through source freshness fields. Derive LIVE/DEGRADED/STALE/UNAVAILABLE and the compatibility `stale` flag consistently. Keep raw Unix timestamps available and do not reinterpret snapshot persistence time as source observation time.
- [ ] **P3.6 — Verify migration and recovery.** Test fresh and legacy databases, repeat startup, migration rollback, unknown timestamps, future timestamps, exact freshness boundaries, one/all/no qualifying sources, partial recovery, and restart persistence. Verify a failed provider retains its quote's original timestamp while healthy providers continue updating. Assert JSON types, nullability, status, and coverage through API tests.

**P4 — Resilient, accessible dashboard (6 tasks)**

Files: `src/ui.html`, `src/ui.rs` if asset organization changes, `src/selenium_tests.rs`, and browser fixtures. Keep the existing Matrix design and embedded delivery.

- [ ] **P4.1 — Make session creation resilient.** Feature-detect `crypto.randomUUID`; use a `getRandomValues` fallback for the presence identifier where needed. Catch storage access failures and keep an in-memory identifier for the page. Handle unavailable browser capabilities with an explicit UI state. Verify distinct live tabs do not unintentionally share a presence identity, including tabs opened or duplicated from another tab.
- [ ] **P4.2 — Control request and visibility lifecycles.** Replace async interval price polling with a single owned loop or in-flight guard. Give each price fetch an abort deadline, and discard responses from an older visibility generation. Keep bounded, nonoverlapping presence heartbeats independent of price fetch completion. Hidden tabs stop timers and send inactive presence; visibility restoration resumes exactly one set of timers. Check `sendBeacon`'s return value and use a keepalive fallback if queuing fails.
- [ ] **P4.3 — Handle failures without losing context.** Check presence HTTP status and catch network failures at the cycle boundary. Display recoverable offline/degraded messaging, retain the last rendered quote with an age warning, and schedule bounded recovery. Ensure non-JSON responses, aborted requests, and initial 503 responses cannot leave the dashboard stuck at Connecting or generate unhandled rejections.
- [ ] **P4.4 — Render honest price status safely.** Display per-source age, quote kind, errors, and aggregate coverage from P3. Use DOM nodes and `textContent` for variable values. Replace the SQLite Snapshot label with Last price update. Refresh displayed ages and expire the LIVE indicator locally between responses; preserve server-provided freshness semantics.
- [ ] **P4.5 — Respect motion and accessibility preferences.** Stop Matrix animation while hidden and honor initial and changing reduced-motion preferences. Mark the decorative canvas appropriately and announce meaningful status transitions without announcing every heartbeat or animation frame. Check narrow layouts and long error/source text.
- [ ] **P4.6 — Add deterministic browser coverage.** Seed data and mock provider endpoints so tests cannot accidentally call public providers after a freshness window expires. Cover first load, stale/partial data, failed presence, delayed prices, hide/show, recovery, unavailable storage/UUID support, reduced motion, and narrow viewport behavior. Assert one in-flight price request per tab and that late responses cannot replace IDLE.

**P5 — Repeatable delivery and deployment (7 tasks)**

Files: new `.github/workflows/ci.yml`, `Cargo.toml`, `Cargo.lock` only if dependencies change, a Rust toolchain file, `Dockerfile`, `.dockerignore`, `src/main.rs`, `src/app.rs`, `src/handlers.rs`, `src/config.rs`, `src/presence.rs`, `scripts/run_selenium_smoke_test.sh`, and `README.md`.

- [ ] **P5.1 — Establish CI early.** Run formatting, Clippy with warnings denied, and locked tests on pull requests. Add a full runtime image build and separate deterministic browser job. Add dependency advisory scanning with visible failures and documented handling of any accepted advisory; pin action versions and use minimal workflow permissions. This task can accompany P1.
- [ ] **P5.2 — Make toolchain and dependency resolution explicit.** Declare a tested minimum Rust version and pin the development/CI toolchain consistently with the image builder; include rustfmt and Clippy. Start from the validated 1.85.1 baseline unless a dependency or security update requires a deliberate change. Use `--locked` in build/test scripts and rebuild the lockfile only for intentional dependency changes. Explicitly declare Tokio features the application uses instead of relying on transitive feature activation.
- [ ] **P5.3 — Run the final image without root.** Use qualified base-image names, a defined application UID/GID, and an owned writable data directory. Keep build tools out of the runtime. Document bind-mount permissions and named-volume behavior, exclude data and unnecessary audit/test artifacts from build context where appropriate, and verify the final slim image rather than only the builder stage.
- [ ] **P5.4 — Add operational lifecycle controls.** Make bind address configurable with validated configuration and useful startup errors. Add a health endpoint that checks local database readability without registering a viewer or making upstream requests; an empty initialized database is healthy. Handle termination signals with a bounded graceful shutdown that lets active transactions finish. Configure a container health probe compatible with the slim image and non-root user.
- [ ] **P5.5 — Bound resource use and improve diagnostics.** Add a configurable viewer cap, prune expired entries before admitting new sessions, and continue accepting heartbeats/removal for existing sessions at capacity. Return a clear capacity response for rejected admissions. Apply bounded request rate/concurrency limits for public hosting without an unbounded limiter map; document proxy trust if IP-based limits are used. Log provider outcome, duration, refresh decisions, and database failures without raw session IDs or sensitive paths in client responses. Verify overload does not bypass refresh cooldowns or prevent cached reads from making progress.
- [ ] **P5.6 — Make the browser runner safe and repeatable.** Pin a tested Selenium image, choose a unique container name by default, and refuse a conflicting supplied name rather than deleting an unrelated container. Clean up only resources created by the current run, including failure paths. Support the containerized Rust workflow used by this audit and document Docker/Podman networking assumptions. Ensure browser sessions, application tasks, and temporary databases are cleaned up when setup or assertions fail.
- [ ] **P5.7 — Document and verify deployment.** Update README with local/container prerequisites, configuration defaults, API examples, freshness/coverage semantics, quote types, migrations, non-root volume setup, single-instance limits, and troubleshooting. Run the complete check suite plus final-image startup, health, pause/resume, restart persistence, and graceful shutdown checks. Record versions, commands, and outcomes here; close the phase only after unresolved deployment failures are addressed.

**Completion gates and evidence**

Each implementation phase must preserve the latest-only storage behavior and have regression coverage for its reported failure. Run the focused tests during development, then these checks before marking the phase complete:

```bash
cargo fmt --all -- --check
cargo clippy --locked --all-targets -- -D warnings
cargo test --locked
git diff --check
```

Run browser tests for frontend/lifecycle changes and build/run the final image for deployment changes. Equivalent release-profile checks are acceptable when reusing the audit's container toolchain; record the actual commands. Do not run public provider calls in deterministic tests. Live-provider compatibility checks, if performed, are separate evidence and must include the check date.

Append one row per completed task or material decision. Keep historical baseline evidence below intact.

| Date | Task or decision | Changes / result | Verification / reference | Next action or blocker |
| --- | --- | --- | --- | --- |
| 2026-09-06 | Baseline audit | Findings and five-phase plan recorded | Commit `cc4a9fb`; baseline validation below | Expand implementation tasks |
| 2026-09-06 | Implementation planning | Added 30 tasks, defaults, dependencies, and completion gates; no application changes | Documentation structure and task counts checked | P1.1: add the SQLite read-race regression |
| 2026-09-06 | P1.1 | Added a deterministic replacement between the metadata and source reads | Regression failed before the fix: expected 1 source, got 0 | P1.2: wrap reads in one transaction |
| 2026-09-06 | P1.2 | Added a deferred read transaction; regression now returns the complete original snapshot | All 5 storage tests pass; changes in `src/storage.rs` | P1.3: confirm rollback and retention |
| 2026-09-06 | P1.3 | Added partial-insert rollback and empty-read tests; strengthened source-order assertions | Rollback retains original metadata and source rows; later replacement succeeds; pruning test passes | P1.4: complete phase checks |
| 2026-09-06 | P1.4 | Completed P1 validation and updated progress to 4/30 | Rust 1.85.1: full suite 10 passed / 1 ignored, formatting, Clippy, and diff checks pass; working-tree changes, not committed | P2.1: add provider/clock test seams |
| 2026-09-06 | P1 commit recorded | P1 changes and tracker committed | `9bce95b` (`p1 complete`) | Continue P2.1 |
| 2026-09-06 | P2.1 | Injected endpoints/time; added local HTTP fixtures, manual time advancement, response gates, request counters, and five tests; progress 5/30 | Rust 1.85.1: full suite 15 passed / 1 ignored; formatting, Clippy, and diff checks pass | P2.2: add production deadlines and structured provider errors |

**Baseline validation performed — September 6, 2026**

- Container builder stage: passed with the Dockerfile's Rust 1.85 image (toolchain 1.85.1), using `podman build --target builder`.
- `cargo test --locked --release`: **7 passed, 0 failed, 1 ignored**.
- `cargo fmt --all -- --check`: passed.
- `cargo clippy --locked --release --all-targets -- -D warnings`: passed.
- Release binary HTTP smoke checks in an ephemeral builder-image container: dashboard returned 200; empty database with no viewers returned 503 and an empty stale response; malformed presence ID returned 400; inactive presence returned 200.
- `bash -n scripts/run_selenium_smoke_test.sh` and inline JavaScript syntax parsing: passed.
- Targeted reproductions: SQLite query interleaving returned old metadata with an empty source list; the actual frontend script permitted overlapping requests, rejected an unhandled presence failure, and failed initialization when UUID support was absent.

Rust was unavailable on the host. Checks ran in containers; formatting and lint components were installed only in an ephemeral container. Podman initially could not resolve the Dockerfile's unqualified Rust image name; pulling `docker.io/library/rust:1.85-bookworm` allowed the unchanged builder stage to complete. The Selenium browser test, final slim runtime image, live provider availability, load testing, and a dependency vulnerability scan were not exercised. Passing helper tests and lint checks does not validate the failure scenarios found by this audit.
